import { Enum, Class, Object } from '@WEB-62221-repro/two'

console.log(Enum.A) // Unresolved variable A
console.log(new Class().bar) // Unresolved variable bar
console.log(Object.foo) // Unresolved variable bar
